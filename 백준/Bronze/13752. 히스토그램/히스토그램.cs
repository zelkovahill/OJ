var T = int.Parse(Console.ReadLine()!);

while (T > 0)
{
    var N = int.Parse(Console.ReadLine()!);

    for (int i = 0; i < N; i++)
    {
        Console.Write("=");
        
    }

    Console.WriteLine();

    T--;
}